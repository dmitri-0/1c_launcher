# 1C Launcher - Лончер баз 1С

GUI-лончер для управления базами данных 1С с поддержкой дерева папок, недавних баз и горячих клавиш.

## Особенности

- 📁 **Дерево папок**: Организация баз в древовидной структуре
- ⏱️ **Недавние базы**: Автоматическое отслеживание последних запущенных баз с сортировкой по времени
- ⌨️ **Горячие клавиши**: Быстрый доступ ко всем функциям
- 🔧 **Настройка баз**: Редактирование параметров подключения, папок, версий
- 📋 **Копирование баз**: Создание дубликатов баз с новым ID
- 🔄 **Чтение ibases.v8i**: Совместимость со стандартным файлом списка баз 1С

## Установка

```bash
# Клонирование репозитория
git clone https://github.com/dmitri-0/1c_launcher.git
cd 1c_launcher

# Установка зависимостей
pip install -r requirements.txt

# Запуск
python main_tree_gui.py
```

## Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| **F1** | Показать справку по горячим клавишам |
| **F3** | Открыть выбранную базу (режим предприятия) |
| **F4** | Открыть конфигуратор для выбранной базы |
| **Ctrl+C** | Скопировать строку подключения в буфер обмена |
| **Ctrl+D** | Копировать базу (создать дубликат) |
| **Ctrl+E** | Редактировать настройки выбранной базы |
| **Esc** | Выход из программы |

> **Примечание**: Для работы горячих клавиш (кроме Esc) необходимо выбрать базу данных в дереве, а не папку.

## Копирование базы (Ctrl+D)

При копировании базы:
- Создаётся новая база с новым UUID
- Сохраняются все параметры исходной базы (имя, строка подключения, папка и т.д.)
- К имени исходной базы автоматически добавляется текущая дата (например: `АСТОР ТЕСТ 2025-11-20`)

## Недавние базы

Папка "Недавние" - это служебная папка скрипта, которая:
- Автоматически отображает базы с флагом `IsRecent=1`
- Сортирует базы по времени последнего запуска (самые свежие первыми)
- **Не сохраняется** в файл `ibases.v8i` (только в памяти)

При запуске базы (F3 или F4) она автоматически помечается как недавняя и перемещается в начало списка.

## Формат файла ibases.v8i

```ini
[АСТОР ТЕСТ]
ID=58a7e941-2cb6-4376-91d9-f106e60b1522
Connect=Srvr="srv-1c-8323:1541";Ref="AstorCO_1017_Pechericadv_2";
Folder=/Тестовые
IsRecent=1
LastRunTime=2025-11-20T11:24:35.123456
App=Auto
Version=8.3.23.2040
AppArch=x86
OrderInTree=110421.333333333
Usr=Печерица Дмитрий Валерьевич
Pwd=3712
```

### Особенности формата:

- `OriginalFolder` - **не сохраняется** в файл (только в памяти для работы)
- `IsRecent=1` - помечает базу как недавнюю
- `LastRunTime` - время последнего запуска в формате ISO 8601
- `Folder` - папка для организации баз в дереве

## Структура проекта

```
1c_launcher/
├── config.py              # Конфигурация (пути к файлам)
├── main_tree_gui.py       # Точка входа скрипта
├── gui/
│   ├── main_tree_window.py # Главное окно с деревом
│   └── ...
├── models/
│   └── database.py         # Модель базы данных 1С
└── services/
    ├── base_reader.py      # Чтение ibases.v8i
    └── ...
```

## Технологии

- **Python 3.8+**
- **PySide6** - GUI фреймворк (Qt for Python)
- **Dataclasses** - модели данных

## Разработка

```bash
# Установка зависимостей для разработки
pip install -r requirements-dev.txt

# Запуск тестов
pytest

# Линтинг
flake8 .
```

## Лицензия

MIT License

## Автор

Разработано для упрощения работы с множеством баз данных 1С.
